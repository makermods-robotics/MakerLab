import React from "react";

/** Placeholder — implemented by worker W2. */
const Collect: React.FC = () => (
  <div>
    <h1 className="text-[22px] font-bold tracking-tight">Collect</h1>
    <p className="mt-1 text-[13.5px] text-muted-foreground">Teach by demonstration — practice with teleop, then record episodes.</p>
  </div>
);

export default Collect;

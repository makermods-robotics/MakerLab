# Python-generated files
__pycache__/
*.py[oc]
/build/
/dist/
/wheels/
*.egg-info

# Virtual environments
.venv
venv/
env/
ENV/

# IDE and Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Environment variables
.env
.env.local
.env.*.local

# Testing
.coverage
.pytest_cache/
.tox/
htmlcov/

# Node.js (in case frontend files leak to root)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/package-lock.json

# Temporary files
*.tmp
*.temp

# Database files
*.db
*.sqlite
*.sqlite3

# Local dev / tool state
.claude/
.playwright-mcp/
.superpowers/
.worktrees/
outputs/
docs/
PHONE_CAMERA_SETUP.md
HTTPS_SETUP.md
JETSON_SETUP.md

# Self-signed dev certs (regenerate per HTTPS_SETUP.md)
certs/

# Design/reference material kept locally, not shipped in the repo. These were
# working material for the redesign (wireframes, concept mockups, spec drafts);
# they stay on disk for whoever has them, but the repo no longer carries them.
reference/
reference-design/

# Jetson host-side material — the patched uvcvideo MJPG kernel driver and its
# setup notes. Machine-specific to the Jetson box, not part of the wheel or the
# app, so it lives locally rather than in the repo.
jetson/
